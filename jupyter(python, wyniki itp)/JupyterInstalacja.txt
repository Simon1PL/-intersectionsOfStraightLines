1. install python
2. install anaconda (z jakiegoś powodu instalując jupyter uzywajac pip nie udało mi się potem zainstalowac mkl aby "import mkl" zadziałało, wiec jak komus sie uda chetnie poslucham jak)
3. albo uzywac Anaconda Prompt zamiast cmd albo aby conda dzialala w cmd dopisac do zmiennych srodowiskowych do path sciezke do anacondy (mozna ja sprawdzic wpisujac where anaconda w Anaconda Prompt). Ja dopisalem: E:\anaconda, E:\anaconda\Library\bin, E:\anaconda\Scripts. dalsze instrukcje wykonywac odpowiedznio w Anaconda Prompt lub w cmd.
4. install jupyter https://jupyter.org/install
5. "conda install matplotlib"
6. "conda install mkl.service"
7. "conda install ffmpeg"
8. odpalic cmd/AnacondaPrompt, przejsc do folderu z plikami jupytera .ipynb i wpisac "jupyter notebook"

uwagi:
-jednoczesnie zainstalowany jupyter przez pip i conde odpalal mi najprawdopodobniej zawsze przez pipa przez co nie dzialal mkl, musialem usunac pliki jupyter z folderu z pipem.
-zapewne część rzeczy napisałem źle a o niekotrych zapomnialem wiec jak cos zapraszam do poprawiania lub na priv
-wszystko instalowalem najnowsze wersje na dzien 17.03.2020

błedy napotkane po drodze:
-jesli wyskakiwal bład odmowa dostepu przy aktualizacji pipa to rozwalalo go i musialem reinstalowac, naprawa nie pomagala
-instalowanie mkl-a przez pipa nie zadziałało nawet z plikami typu "numpy-1.18.1+mkl-cp38-cp38-win_amd64" znalezionymi gdzies na necie